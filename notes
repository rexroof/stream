- friday

today:
* look into chat-bonsai?
* change toggle eton script
- make sub/follow alerts render faster, stick around though
- add sound framework
- sub message doesn't have user, file issue with tau

todo:

- update tauevents object to work with real twitch events
- key combo to fix camera aspect?
- resize browser to not block cameras?
- fix bug with eton-chat script, not opening panes properly


cam errors:
if not result["tmux_window_flags"]:
KeyError: 'tmux_window_flags'
